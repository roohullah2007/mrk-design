# MRK Website — Update Log

## 2026-06-26
- **Growth Partner landing page (`/growth-partner`)**: High-converting subscription/retainer landing page ("MRK Growth Partner — Starting at $200/month"). Sections: hero (ticks + dual CTA), problem/solution, what's included (Design / WordPress / SEO / Creative), 3 pricing tiers (🚀 Growth $200 featured, 📈 Growth Plus $450, 👑 Business Pro $850), how-it-works (4 steps), why MRK, FAQ accordion (`<details>`), pink final CTA. CTAs → /contact + WhatsApp consultation. Route + `PageController@growthPartner`, `Pages/GrowthPartner.jsx`, `.lp-*` styles in app.css. Not yet linked in nav (reachable directly / via ads). (Verified 200 in prod-cached mode.)


A running log of UI/design changes so the whole site structure and design stays documented.

## 2026-06-24

- **Setup**: Cloned repo, installed PHP 8.5 / Composer / Node 26 via Homebrew, ran `composer install`, created `.env` + SQLite DB, ran migrations, `npm install` + `npm run build`. App runs at http://127.0.0.1:8000 with Vite hot-reload.
- **Portfolio (Recent Work) — hover overlay** (`resources/css/app.css`): Added a dark tint overlay that is hidden by default and fades in on hover. `.pf-hover-zoom::after` = `rgba(0,0,0,0.4)`, opacity 0 → 1 on hover; `.pf-click-zoom` (Portfolio 4) gets a matching `brightness(0.6)` on hover (skipped while click-zoomed).
- **Portfolio — first image** (`public/images/Portfolio Section Images/Portolio 1.png`): Replaced original mockup with user's downloaded image (`ab1`). Portfolio 2 & 3 were briefly swapped then reverted to originals.
- **Blog share — WhatsApp + Copy link**: Added two buttons to the "Share this article" row in `BlogPost.jsx`: WhatsApp share (`wa.me/?text=` with post title + URL) and a "Copy link" button (uses `navigator.clipboard` with a textarea fallback, shows a green checkmark "Copied!" state for 2s). Styled in `app.css` (`.share-btn.whatsapp:hover` green, `.share-btn.copy` pink hover / green when copied).
- **Blog share — Open Graph / Twitter Cards**: The "Share this article" buttons (X/LinkedIn/Facebook) were functional (correct share-intent URLs, no SSR so `window` is safe) but shared links had NO preview because there were no OG/Twitter tags, and Inertia's client-side `<Head>` isn't seen by social crawlers. Added server-rendered OG + Twitter Card meta tags in `resources/views/app.blade.php` (per-post title/description/image for the `BlogPost` component via `$page['props']['post']`, sensible site-wide defaults otherwise). Removed the now-redundant client-side meta description from `BlogPost.jsx`. Set `APP_NAME="MRK Design Agency"` in `.env` so og:site_name/title are branded. NOTE: rich previews only generate once deployed to a public domain — localhost URLs aren't reachable by social platforms.
- **Portfolio page — client websites by platform**: Detected the tech stack of 8 client sites and added them as projects with new tabs. WordPress (wp-json/Elementor/generator): doctor-home.com, nicholedaley.com, trusttrevorgroup.com, captivatingconstruction.com (user's "captivatinconstruction.com" typo doesn't resolve — used the correct ...gconstruction.com). Custom Code (no wp markers, nginx): sightflight.io, okbyowner.com, guardmyhvac.com. Shopify (cdn.shopify): captivatingcabinets.com. Captured homepage screenshots (puppeteer, 1440×1125) to `public/images/sites/`. Added "WordPress", "Custom Code", "Shopify" to CATEGORY_ORDER; cards link to the live site ("View live site ↗"). (`Pages/Portfolio.jsx`, `public/images/sites/`)
- **Homepage portfolio — added 2 client sites (paired)**: Added Doctor Home (WordPress) + Guard My HVAC (Custom Code) as a side-by-side split pair (`pf-row-split pf-row-extra`) at the bottom of Frame 1 — matching the East Kent/Meter split style per user request. Uses site screenshots from `public/images/sites/`, links to the live sites, with hover captions. Responsive rule keeps the pair two-up (aspect 320/224) on tablet/mobile. Note: Vite dev server binds IPv6 `[::1]:5173`; HMR works but browser may need a hard refresh to show structural changes. (`Components/Home/Portfolio.jsx`, `app.css`)
- **Homepage portfolio — Masala → East Kent Cardiology**: Replaced the Masala Packaging tile with the East Kent Cardiology Logo & Branding project (heart/stethoscope cover, reused from the earlier download → `behance/east-kent-cardiology.png`), linking to gallery 202737461. (`Components/Home/Portfolio.jsx`)
- **Homepage portfolio — hover captions**: Each homepage thumbnail now shows a hover overlay with the project category, name, and a "View project ↗" link (links to the Behance project / live site). Refactored the tiles into a reusable `Tile` component; added `.pf-info` gradient-caption CSS (fades in on hover, pointer-events none so the whole tile stays clickable). (`Components/Home/Portfolio.jsx`, `app.css`)
- **Homepage portfolio — Perfume Box → Beauty Box SEO**: Replaced the Perfume Box Packaging tile on the homepage with the Beauty Box Qatar SEO project — uses the live-site screenshot `/images/seo/site-beauty-box.jpg` and links to https://www.beautybox.qa/. (`Components/Home/Portfolio.jsx`)
- **Swapped Truckistan Cafe → WpBun Logo (corrected)**: The Truckistan Cafe packaging is actually the file `cafe-branding.jpg` (Behance project 232463915 titled "Cafe Branding Design"). Replaced that project with "WpBun Logo Design" (gallery 229081409, Logo Design) on BOTH the portfolio page and the homepage tile. Cover downloaded (webp→JPEG) as `wpbun-logo.jpg`. Also restored the mislabeled "Packaging Design" (233651181 = Incense Stick Labels) entry that was wrongly changed first. Note: "Branding" category now empty so that tab no longer appears. (`Pages/Portfolio.jsx`, `Components/Home/Portfolio.jsx`)
- **Homepage portfolio — real Behance covers (replaces mockups)**: Per user choice, replaced the 9 generic mockup tiles in the home "Recent Work" section with actual Behance project cover images (from `public/images/behance/`), each linking to its own project so thumbnail = destination. Curated mix: Rad Coffee, Oran Velorian Logo, NEXTGEN Immigration, Fashion Logo, Hockey Coach, Cafe Branding, Perfume Box Packaging, Masala Packaging, Meter Measure Logo. (`Components/Home/Portfolio.jsx`)
- **Homepage portfolio — images link to Behance** (superseded above): Made all 9 tiles in the home "Recent Work" section clickable links to thematically-matched real Behance projects (open in new tab). Converted each `.pf-hover-zoom` div to an `<a>` (added display/link-reset CSS). NOTE: image 4 was a click-to-zoom tile — that interaction was removed so it can be a link like the others (the unused `.pf-click-zoom` CSS remains harmless). Mappings: 1→Deister, 2→Hockey Coach, 3→NEXTGEN Immigration, 4→Street Gear USA, 5→Masala Packaging, 6→Cafe Branding, 7→Perfume Box, 8→Oran Velorian Logo, 9→Fashion Logo. (`Components/Home/Portfolio.jsx`, `app.css`)
- **SEO cards — details always visible**: Made the overlay (name, impressions/clicks metric, "View live site") permanently visible on SEO cards instead of only on hover (`.pp-card--seo .pp-overlay { opacity: 1 }`). Other categories keep hover-reveal. (`app.css`)
- **SEO gallery — website screenshots + proof modal**: Captured live homepage screenshots of the 4 SEO client sites (puppeteer, 1440×1125 ≈ card ratio) to `public/images/seo/site-*.jpg`; these are now the SEO card images. The original GSC dashboard screenshots became the "proof". Added a "View Proof" button (magnifier) on each SEO card that opens a modal showing the Search Console screenshot (title + metric + image), closable via ✕, backdrop click, or Escape (body scroll locked while open). New CSS: `.pp-proof-btn`, `.pp-modal*`. (`Pages/Portfolio.jsx`, `app.css`)
- **Portfolio page — SEO projects from Google Drive**: Pulled 4 SEO case-study screenshots (Google Search Console results) from the user's Drive folder via the Drive MCP and saved to `public/images/seo/` — MG Motors (1.1M impr / 47.4K clicks), Beauty Box Qatar (178K/3.95K), Miele UAE (175K/2.37K), 1847 For Men (118K/3.77K). Added a new "SEO" tab/category; SEO cards show the key metric as a pink pill (`pp-result`) and link to the live client site ("View live site ↗" vs "View on Behance ↗" auto-detected). Wide dashboard screenshots use `object-position: center top`. (The AYjYA .pptx report in the folder was left out — it's a presentation, not a cover image.) (`Pages/Portfolio.jsx`, `app.css`, `public/images/seo/`)
- **Homepage portfolio — replaced last block image**: Replaced the last image (`portfolio 9.png`, rightmost in the final split row — previously IVY) with the user's downloaded heart/stethoscope logo (converted from webp to PNG, kept the filename so no code change). (`public/images/Portfolio Section Images/portfolio 9.png`)
- **Portfolio page — categorized tabs + 2nd Behance profile**: Added 12 more projects from behance.net/mohsinkhan128 (logos, packaging, branding) — 24 total. Added filter tabs (All · Web Design · Logo Design · Packaging · Branding · Funnels · Mobile Apps) with per-tab counts; clicking filters the grid client-side via React state. Each project has a `category` field. Tab styles `.pp-tabs`/`.pp-tab` in `app.css`. (`Pages/Portfolio.jsx`)
- **Portfolio page — hi-res thumbnails**: Re-downloaded all 24 Behance covers at the standard 808×632 thumbnail size (swapped CDN size segment `/projects/404/` → `/projects/808/`; max_1200 redirects/blocked). Set `.pp-media` aspect-ratio to `808/632` to match Behance's native cover ratio. (`public/images/behance/`, `app.css`)
- **Portfolio page — real Behance projects**: Replaced the placeholder projects on `/portfolio` with the user's 12 real Behance projects (Maha Riaz, behance.net/mahakhan10). Downloaded all 12 cover images to `public/images/behance/` (served locally for reliability). Each `.pp-card` is now an `<a>` that opens its Behance gallery in a new tab, with a "View on Behance ↗" label added to the hover overlay. Updated `.pp-card`/`.pp-view` CSS for the anchor. (`Pages/Portfolio.jsx`, `app.css`)
- **Legal pages — Privacy / Terms / Cookies**: Created 3 new pages with full written content: `/privacy-policy`, `/terms-and-conditions`, `/cookies-policy` (routes + `PageController` methods `privacy`/`terms`/`cookies`). Pages live in `resources/js/Pages/Legal/` and share a `Components/Legal/LegalPage.jsx` layout (hero + content, per-page meta description). Added `.legal-*` styles in `app.css`. Linked them from the footer bottom bar (the previously-plain "PRIVACY · TERMS · COOKIES" text is now three `Link`s with inherit color / pink hover). Content references MRK Design Agency, info@mrkdesign.agency, Islamabad PK; governing law Pakistan. (Generic template content — should be reviewed by the client/legal before launch.)
- **Homepage slider — removed an image**: Removed `Rectangle 24.png` (laptop/hands with a video-scrubber overlay) from the `ImageSlider` (below testimonials), per user screenshot. Slider now has 6 images. (`Components/Home/ImageSlider.jsx`)
- **Footer LINKS — added Blog**: Added a "Blog" link (`/blog`) to the footer LINKS column. Also fixed the "Portfolio" link there to point to the real `/portfolio` page (it previously pointed to the home `#work` anchor). (`Components/Layout/Footer.jsx`)
- **Footer — large "MRK®" links home**: Wrapped the big footer wordmark (`.footer-mark`) in a `Link` to `/`. Added `display:block; color:inherit; text-decoration:none` + a subtle hover-opacity so it keeps its look as an anchor. (`Components/Layout/Footer.jsx`, `app.css`)
- **Contact form — Resend mail delivery configured**: Installed `resend/resend-php`, set `MAIL_MAILER=resend` with `RESEND_API_KEY` in `.env` (gitignored), `MAIL_FROM_ADDRESS=noreply@e.mrkdesign.agency`, recipient `CONTACT_TO_ADDRESS=info@mrkdesign.agency`. Verified a live test send through Resend succeeded (no errors). Now real contact-form submissions are delivered to the info@mrkdesign.agency inbox.
- **Contact form — email notifications**: Previously the form ONLY saved to the DB (no email sent). Added `App\Mail\ContactSubmissionMail` (+ `resources/views/emails/contact-submission.blade.php` branded HTML email) and wired `ContactController@store` to email each submission to `config('mail.contact_to')` with Reply-To set to the submitter. Added `mail.contact_to` config (`CONTACT_TO_ADDRESS`, default info@mrkdesign.agency) and set `.env` `CONTACT_TO_ADDRESS=info@mrkdesign.agency` + `MAIL_FROM_ADDRESS=info@mrkdesign.agency`. Mail send is wrapped in try/catch so it never breaks the submission (always stored in DB). ACTION NEEDED for real delivery: `MAIL_MAILER` is still `log` (emails go to storage/logs) — must set real SMTP credentials in `.env` to actually deliver to the inbox. Submissions are also viewable in the Filament admin (`ContactSubmissionResource`).
- **Favicon**: Added the user's downloaded favicon (pink "mrk", 32×32) as `public/favicon.png` and linked it in `app.blade.php` head (`icon`, `shortcut icon`, `apple-touch-icon`). (Previously no favicon `<link>` existed — browsers fell back to the default `favicon.ico`.)
- **Navbar — crisp wordmark logo**: The downloaded logo PNG was only 132×52, so it blurred on retina. Recreated the "mrk / design agency" wordmark as scalable text (Bricolage Grotesque 800, pink "agency") via `.nav-brand` classes — stays sharp at any resolution. (`Components/Layout/Navbar.jsx`, `app.css`)
- **Navbar — new logo image** (superseded above): Replaced the text "MRK" logo with the user's downloaded logo (`mrk design agency` wordmark, pink "agency"), saved to `public/images/mrk-logo.png` (132×52) and shown at 40px height. (`Components/Layout/Navbar.jsx`)
- **Navbar — text logo** (superseded above): Replaced the blurry `Website Navbar Logo.png` image with a text logo "MRK" in Bricolage Grotesque (800, 34px, -0.03em, ink), matching the existing footer logo. (`Components/Layout/Navbar.jsx`)
- **Blog — 3 SEO-optimized posts added**: DB was empty (no posts/categories/users). Created `database/seeders/BlogSeeder.php` (registered in `DatabaseSeeder`) that seeds an author, categories (SEO, Web Design, Branding, Digital Marketing), tags, and 3 published posts with SEO-rich titles, slugs, excerpts, meta_title/meta_description, and HTML content: (1) "SEO for Small Businesses in 2026…", (2) "Web Design That Converts: 7 UX Principles…", (3) "Branding vs. Logo Design…". Ran `storage:link` and copied featured images into `storage/app/public/blog/` (served via `/storage/...`). Seeder is idempotent (updateOrCreate by slug). Run with `php artisan db:seed --class=BlogSeeder`.
- **All pages — consistent footer**: Switched Services and Contact pages from `darkFooter={true}` to the default light footer so every page matches the homepage. (Home, Blog, BlogPost, Portfolio already used the default.)
- **Contact page — banner top spacing**: Added a 96px top margin (56px on mobile) above the "Ready to start?" banner via a scoped `cta-top-gap` class, separating it from the WhyMRK section above. Scoped so the portfolio banner is unaffected. (`Components/Contact/ContactCTA.jsx`, `app.css`)
- **CTA banner pink — specificity fix**: The pink banner background wasn't applying because `.final-cta` (defined later in `app.css`) overrode the equal-specificity `.portfolio-cta-banner` background. Changed all `.portfolio-cta-banner` rules to the compound selector `.final-cta.portfolio-cta-banner` so brand pink reliably wins. Now the contact + portfolio banners both show brand pink. (`resources/css/app.css`)
- **Contact page — "Ready to start?" banner**: Restyled `ContactCTA` to match the portfolio page's pink banner — reuses `final-cta portfolio-cta-banner` classes (brand-pink background, white heading with italic accent, ink "Start a project" pill + white-outline ghost "Or email us directly" pill). (`Components/Contact/ContactCTA.jsx`)
- **Contact form — white card**: Changed the `.form-card` to a white background with dark (ink) text + a border and soft drop-shadow so it stands out on the cream page. Flipped the whole form to a dark-on-white scheme: labels/heading/inputs/budget pills → ink, underlines/placeholders → translucent black, select dropdown arrow → black, decorative `::after` circle + required asterisk + input focus + checked pill + "Sent" state → brand pink accent, submit button → ink-black with white text (hover → pink). (Previously tried pink card — not visible enough.) (`resources/css/app.css`, `Components/Contact/ContactForm.jsx`)
- **Portfolio page — footer**: Switched `MainLayout` to the default (light) footer to match the homepage (was `darkFooter={true}`).
- **Portfolio page — CTA banner pink**: Changed the "Have a project in mind?" banner background to brand pink (`--accent` #E42278); recolored eyebrow/heading/subtext to white and made the primary pill ink-black so accent elements stay visible on pink.
- **Portfolio page — CTA banner**: Restyled the bottom "Have a project in mind?" heading + CTA as a dark banner matching the homepage `FinalCTA` (reuses `.final-cta` classes: eyebrow "LET'S BUILD", big heading with italic accent, subtext, "Start a project" + "Book a 30-min intro call" pills). Added `.portfolio-cta-banner` override (solid ink, padding-based height) and removed the old `.pp-cta` styles.
- **New page: Full Portfolio** (`/portfolio`): Added route (`routes/web.php`) + `PageController@portfolio` returning Inertia `Portfolio` page. New `resources/js/Pages/Portfolio.jsx` — hero ("Our recent work") + responsive 3-col gallery grid of all 9 portfolio images with gradient hover overlay (tag + title) and a "Start a project" CTA. Wired the home "View full portfolio" button (`Components/Home/Portfolio.jsx`) to link to `/portfolio`. Styles added under "Full Portfolio page" in `app.css` (`.portfolio-page-hero`, `.pp-grid`, `.pp-card`, etc.).
- **Portfolio — last row split** (`resources/js/Components/Home/Portfolio.jsx`, `app.css`, images): The last item was a single combined image (Blocks + IVY in one 590×243 file). Split into two separate side-by-side image boxes: `portfolio 8.png` = Blocks (Rectangle 29), `portfolio 9.png` = IVY (Rectangle 30). Last `.pf-row` now `pf-row-split` with two equal `pf-hover-zoom` boxes (`flex: 1 1 0`); responsive @media rules for that row updated for two-up layout.
